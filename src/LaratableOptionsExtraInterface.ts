/**
 * LaratableOptionsExtraInterface
 * enforces how Laratable custom options should be set.
 *
 * @interface LaratableOptionsExtraInterface
 */
interface LaratableOptionsExtraInterface {
  searchSelector?: string;
}

export default LaratableOptionsExtraInterface;
